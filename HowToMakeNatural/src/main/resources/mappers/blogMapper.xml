<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blogMapper">
	<!-- 블로그 메인 페이지의 글 가져오기 -->
    <select id="selectAllPost" parameterType="hashmap" resultType="com.my.vo.blogVO" >
      		SELECT * FROM blog
      		<if test='category != null and category != ""'>
			where category=#{category}
 			</if>
      		order by no desc limit #{page},10
    </select>
    <select id="selectPost" resultType="com.my.vo.blogVO" >
      		SELECT * FROM blog where category=#{category} order by no desc
    </select>
    <select id="selectOnePost" resultType="com.my.vo.blogVO" >
      		SELECT * FROM blog where no=#{no}
    </select>
    <insert id="insertPost" parameterType="com.my.vo.blogVO">
    	insert into blog(no, text, category, writerID, signdate) values (#{name}, #{text}, #{category}, #{writerID}, NOW())
    </insert>
    <update id="updatePost">
    	update blog set category=#{category},text=#{text},signdate=NOW() where no=#{no}
    </update>
    <delete id="deletePost">
    	delete from blog where no=#{no} and writerID=#{writerID}
    </delete>
    <delete id="deleteCategory">
    	delete from blog where category=#{writerID} and userID=#{writerID}
    </delete>
</mapper>