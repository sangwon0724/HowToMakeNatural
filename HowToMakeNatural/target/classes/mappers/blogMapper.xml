<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blogMapper">
	<!-- 블로그 메인 페이지의 글 가져오기 -->
    <!-- 아이디 값 존재 O => 해당 유저의 블로그에 대한 글  가져오기 -->
    <!-- 아이디 값 존재 X => 블로그 메인에 대한 글 가져오기 -->
    <select id="selectPost" parameterType="hashmap" resultType="com.my.vo.blogVO" >
      		SELECT * FROM blog
      		<if test='userID != null and userID != ""'>
				where userID=${userID}
				<if test='category != null and category != ""'>
					and category='%${category}%' or tag like '%${category}%'
 				</if>
 			</if>
      		<if test='userID == null or userID == ""'>
				<if test='category != null and category != ""'>
					where category like '%${category}%' or tag like '%${category}%'
 				</if>
				<if test='object != null and object != ""'>
					<if test='object =="post"'>
						where title like '%${search}%' or content like '%${search}%'
	 				</if>
					<if test='object =="theme"'>
						where category like '%${search}%' or tag like '%${search}%'
	 				</if>
					<if test='object =="writer"'>
						where userID like '%${search}%' or userNickName like '%${search}%'
	 				</if>
 				</if>
 			</if>
      		order by no desc limit ${page*10},10
    </select>
    
    <!-- 블로그 게시글의 개수 가져오기 -->
    <!-- 아이디 값 존재 O => 해당 유저의 블로그에서 글 개수 가져오기 -->
    <!-- 아이디 값 존재 X => 블로그 메인에서 글 개수 가져오기 -->
    <select id="selectCount" parameterType="hashmap" resultType="java.lang.Integer" >
      		SELECT COUNT(*) FROM blog
      		<if test='userID != null and userID != ""'>
				where userID=${userID}
				<if test='category != null and category != ""'>
					and category='%${category}%' or tag like '%${category}%'
 				</if>
 			</if>
      		<if test='userID == null or userID == ""'>
				<if test='category != null and category != ""'>
					where category like '%${category}%' or tag like '%${category}%'
 				</if>
 			</if>
    </select>
    
    <select id="selectOnePost" resultType="com.my.vo.blogVO" >
      		SELECT * FROM blog where no=#{no}
    </select>
    <insert id="insertPost" parameterType="com.my.vo.blogVO">
    	insert into blog(no, text, category, writerID, signdate) values (#{name}, #{text}, #{category}, #{writerID}, NOW())
    </insert>
    <update id="updatePost">
    	update blog set category=#{category},text=#{text},signdate=NOW() where no=#{no}
    </update>
    <delete id="deletePost">
    	delete from blog where no=#{no} and writerID=#{writerID}
    </delete>
</mapper>